/* Estilos generales para el cuerpo de la página */
body {
    font-family: 'Arial', sans-serif;
    background: linear-gradient(to right, #87CEEB, #32CD32); /* Fondo degradado que imita el cielo */
    color: #ffffff; /* Color de texto blanco para un buen contraste */
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    height: 100%;
}

/* Encabezado */
header {
    text-align: center;
    padding: 40px 20px;
    background-color: rgba(0, 0, 0, 0.6); /* Fondo semitransparente para encabezado */
    color: #fff;
    font-size: 36px;
    text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.7);
}

/* Contenedor principal */
.container {
    padding: 20px;
    max-width: 1200px;
    margin: 0 auto;
    background-color: rgba(255, 255, 255, 0.8); /* Fondo semitransparente */
    border-radius: 15px;
}

/* Títulos principales */
h1, h2, h3 {
    text-align: center;
    font-weight: bold;
    text-shadow: 1px 1px 5px rgba(0, 0, 0, 0.6);
}

/* Fondo de cada sección */
.section {
    padding: 40px 20px;
    margin: 40px 0;
    border-radius: 10px;
    background-color: rgba(0, 0, 0, 0.6);
}

/* Estilo de los botones */
button {
    background-color: #87CEEB; /* Azul claro */
    border: none;
    color: white;
    padding: 15px 32px;
    text-align: center;
    text-decoration: none;
    display: inline-block;
    font-size: 18px;
    border-radius: 8px;
    cursor: pointer;
    transition: background-color 0.3s, transform 0.1s;
    margin: 10px 0;
}

/* Efecto de cambio de color cuando el ratón pasa por encima de los botones */
button:hover {
    background-color: #4682B4; /* Azul más oscuro */
}

/* Efecto de reducción del tamaño del botón al hacer clic */
button:active {
    transform: scale(0.95); /* Efecto de presión */
}

/* Estilo de las imágenes */
img {
    width: 100%;
    height: auto;
    border-radius: 10px;
    opacity: 0;
    transition: opacity 1s ease-in-out; /* Transición de desvanecimiento */
}

/* Clases para hacer que las imágenes se desvanecen cuando estén visibles */
img.fade-in {
    opacity: 1;
}

/* Estilo para el texto de los enlaces */
a {
    color: #32CD32;
    text-decoration: none;
    transition: color 0.3s;
}

a:hover {
    color: #fff; /* Cambia el color de los enlaces al pasar el ratón */
}

/* Estilo de los enlaces internos (con anclas) */
section a {
    font-weight: bold;
}

/* Animación de deslizamiento suave para el contenido */
.scrollable {
    transition: transform 0.5s ease;
}

/* Efecto de imagen de fondo en las secciones */
.section img {
    opacity: 0.3;
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: -1;
}

/* Estilo de las cajas de información (para los temas) */
.box-info {
    background-color: rgba(255, 255, 255, 0.7);
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.4);
    margin-bottom: 30px;
}

.box-info h3 {
    font-size: 24px;
    margin-bottom: 10px;
}

/* Botón de información adicional */
#btn-info {
    background-color: #32CD32; /* Color verde */
    color: white;
    border-radius: 8px;
    border: none;
    padding: 15px 32px;
    font-size: 18px;
    cursor: pointer;
    transition: background-color 0.3s ease, transform 0.1s;
}

/* Estilo del pie de página */
footer {
    background-color: #000;
    color: white;
    text-align: center;
    padding: 10px 0;
    position: fixed;
    width: 100%;
    bottom: 0;
}

footer p {
    margin: 0;
    font-size: 14px;
}

/* Estilos de carga de imágenes */
.loading {
    display: block;
    margin: 20px auto;
    font-size: 20px;
    color: #32CD32;
}


